services:
  - type: web
    name: flaremo-tools
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn tools_hub.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DEBUG
        value: "False"